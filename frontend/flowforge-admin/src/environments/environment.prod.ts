export const environment = {
  production: true,
  apiUrl: '/api/v1',
  // No production deployment exists yet (see README "Known gaps") - update this to
  // wherever the main app actually ends up hosted.
  mainAppUrl: 'https://app.flowforge.example.com'
};
